<ul>
  <% unless params[:action] == "index"%> <li><%= link_to 'Index', admin_inkling_folders_path %></li> <% end %>
  <li><%= link_to 'Create', new_admin_inkling_folder_path %></li>
  <% unless ["create", "index", "new"].include?(params[:action]) %>
    <li>
      <%= link_to 'Delete',  admin_inkling_folder_path(@folder), :method => :delete, :confirm => "Are you sure? Deletion is permanent." %>
    </li>
  <% end %>

</ul>
