
<%= error_messages_for 'foo'%>

<%= form_for [:admin, @foo] do |f| %>
  <% unless @foo.new_record? %>
    <%= f.hidden_field :id %>
  <% end %>

  <%= f.hidden_field :parent_folder_id => params[:parent_folder_id] if params[:parent_folder_id] %>

  <%= f.text_field :name, :label => "Name" %>

  <%= submit_tag 'Save' %>
<% end %>
