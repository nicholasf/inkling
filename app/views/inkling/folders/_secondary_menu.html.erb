<ul>
  <% unless params[:action] == "index"%> <li><%= link_to 'Index', folders_path %></li> <% end %>
  <li><%= link_to 'Create', new_folder_path %></li>
  <% unless ["create", "index"].include?(params[:action]) %>
    <li>
      <%= link_to 'Delete',  inkling_folder_path(@folder), :method => :delete, :confirm => "Are you sure? Deletion is permanent." %>
    </li>
  <% end %>

</ul>
